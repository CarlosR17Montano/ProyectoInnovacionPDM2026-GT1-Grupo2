<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fondo blanco para mejorar lectura -->
    <solid android:color="#FFFFFF" />

    <!-- Borde rosado de la app -->
    <stroke
        android:width="1.5dp"
        android:color="#D81B60" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Espaciado interno -->
    <padding
        android:left="14dp"
        android:right="14dp"
        android:top="10dp"
        android:bottom="10dp" />

</shape>