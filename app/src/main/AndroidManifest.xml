<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para usar la camara del dispositivo -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Permiso para usar internet y cargar Google Maps -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permisos para obtener ubicacion del dispositivo -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permite mostrar notificaciones en Android 13 o versiones superiores -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Indica que la app puede usar camara si el equipo tiene -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoInnovacionPDM2026GT1Grupo2"
        tools:targetApi="31">

        <!-- API Key de Google Maps tomada desde local.properties -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Pantallas secundarias de la aplicacion -->
        <activity android:name=".EvidenciaActivity" />
        <activity android:name=".MapaActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".RepartidorActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".HistorialPedidosActivity" />
        <activity android:name=".CrearPedidoActivity" />
        <activity android:name=".SeleccionarUbicacionActivity" />
        <activity android:name=".SolicitudesRepartidorActivity" />

        <!-- Pantalla inicial de la aplicacion -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>